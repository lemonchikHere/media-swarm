version: '3.9'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data

  app:
    build: .
    env_file: .env
    depends_on:
      - redis
    volumes:
      - ./:/app
    command: python -m src.scheduler.runner

volumes:
  redis_data:
